# Eval: usememos/memos

Knowledge files generated by running the `analyze-codebase` skill against
[usememos/memos](https://github.com/usememos/memos) (commit `0e9e94e`, branch `main`, 2026-08-29).

## Purpose

This eval exists so a second Bob instance can verify the skill's output accuracy
independently. The reviewer should:

1. Run `/analyze-codebase` against `usememos/memos` on a fresh instance.
2. Compare the freshly generated files against the ones in this directory.
3. Use `AGENTS-comparison.md` as the reference for how the skill evaluated the existing
   `AGENTS.md` at the project root.

## Files

| File | Description |
|---|---|
| `PROJECT.md` | Project overview, purpose, features, deployment |
| `DEPENDENCIES.md` | Go and frontend dependency inventory |
| `ARCHITECTURE.md` | System structure, request lifecycle, component map |
| `CONVENTIONS.md` | Go and frontend coding conventions, linting rules |
| `TESTING.md` | Test groups, patterns, CI configuration |
| `GLOSSARY.md` | Domain terms, enums, resource name conventions |
| `AGENTS-comparison.md` | Comparison of generated candidate vs existing root `AGENTS.md` |
