# Copy this file to .env and fill in the values.
# Never commit .env — it is gitignored.

# IBM Cloud — Cloudant (set via GitHub Actions org secrets in CI)
CLOUDANT_URL=https://<instance-id>-bluemix.cloudantnosqldb.appdomain.cloud
CLOUDANT_API_KEY=<iam-api-key>
